# Admin dashboard — Cloudflare Pages (copy to admin/.env.local for dev)

# Required for production / Cloudflare Pages builds
NEXT_PUBLIC_API_URL=https://api.example.com/api

# Optional — public CDN hostname for uploaded images
# NEXT_PUBLIC_IMAGES_HOST=cdn.example.com
